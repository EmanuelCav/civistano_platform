import { Text, View } from 'react-native'

import { RegisterPropsType } from '../../types/questionnnaire.types'

const Register = ({}: RegisterPropsType) => {
    return (
        <View>
            <Text>Register</Text>
        </View>
    )
}

export default Register